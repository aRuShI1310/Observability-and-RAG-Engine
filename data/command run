python -m streamlit run app_streamlit.py

sample queries : 
💡 How to fix it right now (No code changes needed)
To get working results immediately without rewriting your ingestion system, test your RAG app with queries that actually target anomalies and summaries.

Try typing these exact queries into your Streamlit interface:

Summary Query: "Give me a summary of the performance metrics for the checkout-api on February 14, 2025."

Anomaly Query: "Were there any high latency or error rate anomalies reported for the payment-service in production?"

General System Query: "What issues or spikes were recorded in the daily summaries for the staging environment?"

( 🔍 The Root Cause
1. You are searching the wrong data type
Your ingest_csv.py file is just a processing utility—it cleans the raw CSV logs but doesn't actually store them in ChromaDB. Based on your prompt template ("retrieved context about metrics, anomalies, and daily summaries") and your sidebar filters (doc_type = st.selectbox("Type", ["any", "anomaly", "summary"])), ChromaDB only contains high-level anomalies and summaries generated by another script (likely daily_rollups.py or windowing and eventization routines).)